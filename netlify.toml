# ค่าตั้งของ Netlify — บอกว่า "เว็บจริง" เอาจากโฟลเดอร์ site/
# ไฟล์นี้ทับค่าตั้งในหน้าเว็บ Netlify เสมอ — ห้ามลบ
[build]
  publish = "site"

# ทุกหน้าที่หาไม่เจอให้กลับไปหน้าร้าน (กันพิมพ์ URL ผิดแล้วเจอ 404 เปล่า ๆ)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
